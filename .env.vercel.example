# ============================================
# Heres Protocol — Vercel Environment
# Copy these to Vercel Dashboard > Settings > Environment Variables
# ============================================

# --- Solana (Required) ---
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_PROGRAM_ID=AmiL7vEZ2SpAuDXzdxC3sJMyjZqgacvwvvQdT3qosmsW
NEXT_PUBLIC_PLATFORM_FEE_RECIPIENT=Covn3moA8qstPgXPgueRGMSmi94yXvuDCWTjQVBxHpzb
NEXT_PUBLIC_HELIUS_API_KEY=            # optional: Helius RPC for better reliability

# --- Crank (Required for auto-execute cron) ---
CRANK_WALLET_PRIVATE_KEY=<YOUR_CRANK_WALLET_PRIVATE_KEY>
CRON_SECRET=<generate-random-string>   # Vercel cron auth + CRE dispatch fallback

# --- CRE: Mock mode on Vercel (no real Chainlink) ---
# Points to itself — mock endpoint auto-callbacks with "delivered"
CHAINLINK_CRE_WEBHOOK_URL=https://<your-vercel-domain>/api/mock/cre
CHAINLINK_CRE_SIGNING_SECRET=<generate-random-string>
CHAINLINK_CRE_CALLBACK_SECRET=<generate-random-string>
MOCK_CRE_AUTO_CALLBACK=true
MOCK_CRE_CALLBACK_BASE_URL=https://<your-vercel-domain>

# --- Optional ---
CRE_DISPATCH_SECRET=                  # separate dispatch API auth (falls back to CRON_SECRET)
OPS_ALERT_WEBHOOK_URL=                # Slack/Discord webhook for failure alerts

# ============================================
# LOCAL DEV uses CRE simulate mode (real email via Resend):
#   RESEND_API_KEY=re_xxxx
#   RESEND_FROM_EMAIL=Heres Protocol <onboarding@resend.dev>
# Vercel uses mock mode (no real email, just callback simulation)
# ============================================
